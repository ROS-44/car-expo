# Voir README.md, section "Rendre la disponibilité modifiable sans redéployer".
# Copie ce fichier en `.env` en local et renseigne l'URL de ton Apps Script.
# Sur Netlify, la même variable se règle dans Site settings > Environment variables.

VITE_GOOGLE_SHEET_API_URL=
